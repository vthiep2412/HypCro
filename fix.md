- Fix ESP didn't apply to pest as far and fast like glow in skyblocker /learn/ skyblocker

- Plot 0 in scoreboard will show as "The Garden" Case sensitive, so rename bot detection

- Fix to really make null pos doesn't like minecraft or add boolen as doesn't care about null, so pathfinding when find a whole that lead to the sky, it go straight up (like for ~ 82 ~, true), instead of have to go back to the ~, ~, and please follow me instead of the damn minecraft thingy

- 1.8s is too bad, reduce to 1.5s, and when the macro detect in turn it (check macro in turn before alarm) and if in turn it has to wait 500ms before going back to check!